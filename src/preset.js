/**
 * Storybook preset for design token tables
 */
module.exports = {
  // In a real addon, you might add babel or webpack configurations
  // Register the addon manager and/or decorator if needed
  managerEntries: [],
  // We don't need to add preview entries because the tokens block will be imported directly
  // into mdx files
};
